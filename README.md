# learning-github-
# My Cybersecurity Journey

</> MARKDOWN
Cybersecurity-Portfolio
## skills
├── Linux = cd, ls, mkdir, touch, cd ..,nano, nmap, cat, bash, ssh, ls -a, ls -h, pwd, echo, clear, etc.
├── Networking = ip config,  who am i, 
├── Python
├── Bash Scripts = git branch
git checkout -b new-feature
git merge

├── TryHackMe Notes = cybersecurity room101
└── README.md
